# Example environment variables for AI Email Summarizer
# Copy to .env and set real values before running (do NOT commit .env to repo)

# Your Gemini API key (Google Generative Language API)
GEMINI_API_KEY=YOUR_GEMINI_API_KEY_HERE

# Optional: path to OAuth credentials JSON (if not in root)
# OAUTH_CREDENTIALS_PATH=credentials.json
